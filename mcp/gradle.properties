# Important Notes:
# Every field you add must be added to buildSrc/src/main/groovy/multiloader-common.gradle expandProps map.

# Project
version=0.1.0
group=com.dwinovo.numen
java_version=21

# Common
minecraft_version=1.21.1
mod_name=Numen MCP
mod_author=dwinovo
mod_id=numen_mcp
license=MIT
credits=
description=Exposes Numen companions as an MCP (Model Context Protocol) server, so an external agent like Claude can list, take control of, and directly drive companion bodies — calling their tools without the built-in LLM. Hand-rolled JSON-RPC over the JDK HTTP server; zero third-party dependencies (Gson is on the MC classpath).
minecraft_version_range=[1.21.1, 1.21.2)
## This is the version of minecraft that the 'common' project uses, you can find a list of all versions here
## https://projects.neoforged.net/neoforged/neoform
neo_form_version=1.21.1-20240808.144430

# Fabric, see https://fabricmc.net/develop/ for new versions
fabric_version=0.116.7+1.21.1
fabric_loader_version=0.18.1

# NeoForge, see https://projects.neoforged.net/neoforged/neoforge for new versions
neoforge_version=21.1.233
neoforge_loader_version_range=[4,)

# The bridge itself uses java.net.http (JDK built-in WebSocket, Java 11+) and
# Gson (already on the MC classpath). No third-party HTTP / bot-framework deps.

# Gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
